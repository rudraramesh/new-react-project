const initialData={
    cartcount:0
}

const cartRoducer=(state=initialData,action) =>{
    // state store the initial value
    // action is a function which perform operation

    return state
}

export default cartRoducer